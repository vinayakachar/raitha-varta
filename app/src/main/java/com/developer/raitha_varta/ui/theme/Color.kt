package com.developer.raitha_varta.ui.theme

import androidx.compose.ui.graphics.Color

val ForestGreen = Color(0xFF1B5E20)    // Primary: Header and Auth
val EmeraldAction = Color(0xFF10B981) // Secondary: Flash-Card Action box
val DeepPlantGreen = Color(0xFF064E3B) // Tertiary: High-contrast text

// Functional Colors
val UrgentRed = Color(0xFFEF4444)      // Warning/Emergency badges
val OffWhite = Color(0xFFF9FFF9)       // App background for clean look

// Keeping these for internal system defaults if needed
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)